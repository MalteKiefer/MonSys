// Barrel for the split Notifications pages. Replaces the old
// pages/AdminNotifications.tsx monolith. Each page renders its own header
// (via components/page) and the shared NotificationsTabs sub-nav.

export { RulesPage } from "./RulesPage";
export { ChannelsPage } from "./ChannelsPage";
export { AlertsPage } from "./AlertsPage";
